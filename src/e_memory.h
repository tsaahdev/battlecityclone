#pragma once

#include "e_types.h"

namespace base::memory {

// u8* allocate(size_t ammount) {
//  // again... it is not a teture manager ffs /sigh
//     // return static_cast<u8*>(malloc(ammount));
// }

// u8* reallocate() {
// }

} // namespace base::memory

